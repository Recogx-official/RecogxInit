import {
  __toESM,
  require_react
} from "./chunk-TL73C5TT.js";

// node_modules/react-ts-typewriter/dist/Typewriter.js
var import_react = __toESM(require_react());
import styles from "/home/h1/asp/js/Recogx/node_modules/react-ts-typewriter/dist/Typewriter.module.css";
var DEFAULT_MS = 30;
function Typewriter({ text, speed = DEFAULT_MS, loop = false, random = DEFAULT_MS, delay = DEFAULT_MS, cursor = true, onFinished = () => {
}, onStart = () => {
} }) {
  const [currentStringIndex, setCurrentStringIndex] = (0, import_react.useState)(0);
  const [currentTextIndex, setCurrentTextIndex] = (0, import_react.useState)(0);
  if (!Array.isArray(text))
    text = [text];
  (0, import_react.useEffect)(() => {
    setTimeout(() => {
      if (currentTextIndex === 0)
        onStart();
      if (currentTextIndex < text[currentStringIndex].length) {
        setCurrentTextIndex(currentTextIndex + 1);
      } else {
        if (currentStringIndex < text.length - 1) {
          setTimeout(() => {
            setCurrentTextIndex(0);
            setCurrentStringIndex(currentStringIndex + 1);
          }, delay);
        } else {
          if (loop) {
            setTimeout(() => {
              setCurrentTextIndex(0);
              setCurrentStringIndex(0);
            }, delay);
          } else {
            onFinished();
          }
        }
      }
    }, speed + Math.random() * random);
  });
  return import_react.default.createElement(
    "span",
    null,
    text[currentStringIndex].substring(0, currentTextIndex),
    import_react.default.createElement("span", { className: styles.cursor }, cursor && "\u258E")
  );
}
export {
  Typewriter as default
};
//# sourceMappingURL=react-ts-typewriter.js.map
